#pragma once
#include <vector>
#include <string>
#include <iostream>
#include <sstream>
using namespace std;


vector<string> Split(char* str, char ch, bool noSpace = true);

int ParseInt(string str);